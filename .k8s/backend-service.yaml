apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: book-management-backend
spec:
  selector:
    app: book-management-backend
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP